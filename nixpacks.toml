[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @quotatain/database db:generate", "pnpm --filter @quotatain/api build"]

[start]
cmd = "node apps/api/dist/index.js"
